<mvc:View
        controllerName="sasha.koroliova.app.controller.NotFound"
        xmlns:mvc="sap.ui.core.mvc"
        xmlns="sap.m">
    <MessagePage
            title="{i18n>NotFound}"
            text="{i18n>NotFound.text}"
            icon="{sap-icon://document}"
            id="page"
            description="{i18n>NotFound.description}"
            showNavButton="true"
            navButtonPress="onNavBack">
    </MessagePage>
</mvc:View>